services:
    redis:
        image: redis:7
        container_name: redis
        ports:
            - '6379:6379'

    scrapy:
        build: .
        container_name: scrapy
        depends_on:
            - redis
